apiVersion : apps/v1
kind: NginxIngress
metadata:
  name: ingress
  namespace: nginx-ingress
spec:
  controller:
    image:
      pullPolicy: IfNotPresent
      repository: nginx/nginx-ingress
      tag: 3.0.0-ubi
    ingressClass: nginx
    kind: deployment
    nginxplus: false
    replicaCount: 1